# local dev: use localhost (NOT 127.0.0.1) so the frontend and backend are the
# same site and the session cookie is kept by the browser
VITE_API_URL=http://localhost:8000

# production value is set as a Railway service variable on the frontend service:
# VITE_API_URL=https://api.<your-domain>
